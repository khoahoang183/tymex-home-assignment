<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/color_white">

    <androidx.fragment.app.FragmentContainerView
        android:id="@+id/mainFcv"
        android:name="androidx.navigation.fragment.NavHostFragment"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:defaultNavHost="true"
        app:layout_constraintBottom_toTopOf="@id/bottomNavView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:navGraph="@navigation/nav_graph_home_nested" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavView"
        android:layout_width="match_parent"
        android:layout_height="@dimen/view_height_bottom_navigation"
        android:background="@color/color_main"
        app:itemBackground="@drawable/selector_state_color_bg"
        app:itemIconSize="@dimen/long_28dp"
        app:itemIconTint="@drawable/selector_state_color"
        app:itemRippleColor="@null"
        app:itemTextAppearanceActive="@style/Text.BottomNavigation.Active"
        app:itemTextAppearanceInactive="@style/Text.BottomNavigation.Inactive"
        app:itemTextColor="@drawable/selector_state_color"
        app:layout_constraintBottom_toBottomOf="parent"
        app:menu="@menu/nav_menu_home_bottom"
        app:labelVisibilityMode="selected"
        app:tabBackground="@drawable/selector_state_color_bg" />

</androidx.constraintlayout.widget.ConstraintLayout>